title: ErrorResponse
required:
  - statusCode
  - errorMessage
properties:
  statusCode:
    type: integer
    description: http status code
  errorMessage:
    type: string
    description: Reason for error request
